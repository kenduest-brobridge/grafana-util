This directory holds maintainer-only demo scripts for features that are still
intentionally unwired from the public `grafana-util` CLI.

These files are examples of direct Python API usage, not supported operator
entrypoints.
