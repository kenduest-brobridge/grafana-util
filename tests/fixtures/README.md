# Shared Test Fixtures

This directory holds shared contract and golden-case fixtures used by Rust,
Python, schema manifests, and maintainer validation scripts.

Keep these files runtime-neutral. Do not use this directory for scratch exports,
local Grafana dumps, or generated build artifacts.
